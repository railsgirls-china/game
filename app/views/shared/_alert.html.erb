<% flash.each do |type, msg| %>
  <div class="<%= alert_type(type) %>">
    <i class="close icon"></i>
    <p><%= msg %></p>
  </div>
<% end %>

<script>
  $('.message .close').on('click', function() {
    $(this).closest('.message').fadeOut();
  });
</script>
